{
  "name": "impactbyte-forum",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd api && npm install && npm start",
    "deploy": "git push heroku master"
  },
  "engines": {
    "node": "9.x.x"
  }
}
